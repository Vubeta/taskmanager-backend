# ===========================
# = APPLICATION INFO        =
# ===========================
spring.application.name=taskmanager

# ===========================
# = DATABASE CONFIGURATION  =
# ===========================
spring.datasource.url=jdbc:mysql://localhost:3306/task_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=2002040111Vu

# ===========================
# = JPA / HIBERNATE CONFIG  =
# ===========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ===========================
# = CORS CONFIG             =
# ===========================
app.cors.allowed-origins=http://localhost:5173

# ===========================
# = JWT CONFIG              =
# ===========================
app.jwt.secret=${APP_JWT_SECRET:change_this_secret}
app.jwt.expirationMs=${APP_JWT_EXPIRATIONMS:86400000}
